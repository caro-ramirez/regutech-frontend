export default function ComingSoon({ label }) {
  return (
    <div className="rt-panel p-4">
      <h1 className="fs-5 fw-semibold mb-1">{label}</h1>
      <p className="rt-muted small mb-0">
        Esta pantalla todavía no está implementada. Placeholder para armar el flujo de navegación.
      </p>
    </div>
  );
}
